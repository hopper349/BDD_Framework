using System;
using System.Diagnostics;
using AutoFrameWork.Base;
using AutoFrameWork.COnfig;
using AutoFrameWork.Helpers;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;
using OpenQA.Selenium.Firefox;
using OpenQA.Selenium.IE;
using UnitTesting.Pages;

namespace UnitTesting
{/*
    [TestClass]
    public class UnitTest1: HookInitialize
    {
      

   

        [TestMethod]
        public void TestMethod1()
        {
           

            string fileName = Environment.CurrentDirectory.ToString() + "\\Datasheet\\Login.xlsx";
            
            ExcelHelpers.PopulateInCollection(fileName);
           
           
            
            CurrentPage = GetInstance<LoginPage>();
            CurrentPage.As<LoginPage>().ClickLnkLogin();
            CurrentPage.As<LoginPage>().LoginCred(ExcelHelpers.ReadData(1,"UserName"), ExcelHelpers.ReadData(1, "Password"));
            
            // CurrentPage.As<HomePage>().ClkManageAssociation();
        }
      
        
        }*/
}
